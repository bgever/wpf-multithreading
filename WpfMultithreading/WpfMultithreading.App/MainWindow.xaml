<Window x:Name="window" x:Class="WpfMultithreading.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfMultithreading.App"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DataGrid ItemsSource="{Binding OutputEntries}" DataContext="{Binding Mode=OneWay, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:MainWindow}}}">
        <!--<DataGrid.Columns>
            <DataGridTextColumn Header="Timestamp"/>
            <DataGridTextColumn Header="Message" />
            <DataGridTextColumn Header="ThreadLabel" />
        </DataGrid.Columns>-->
    </DataGrid>
</Window>
